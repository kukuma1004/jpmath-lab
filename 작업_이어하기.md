<!-- 문서-권한 -->
> **작업 일지**  현재 기준은 [`JP_MATH_LAB_2.0_MASTER_SPEC.md`](JP_MATH_LAB_2.0_MASTER_SPEC.md) 하나다.
> 명세가 아니다. 어디까지 했고 어떤 함정을 밟았는지 남긴 기록이다.
> _(2026-08-31 문서 권한 정리)_

# 작업 이어하기

## 2026-09-09 (4) · 세계시장 원정 친구방 LIVE 이식 + 사이트 연결

### 먼저 — 남은 것은 규칙 재게시 하나뿐이다 (선생님이 하실 일)

배포된 사이트에서 실제로 확인했다. **인증과 데이터베이스는 이미 정상이다.**

```
connect()  → available: true, uid 발급됨, asia-southeast1 연결
createRoom → 성공 (방 생성까지 됨)
```

익명 로그인은 켜져 있고, GitHub Pages 에서 잘 붙는다.
기존 LIVE 게임 8개의 인프라도 이 경로로 살아 있다는 뜻이다.

> **한때 "승인된 도메인에 kukuma1004.github.io 가 없어서 막힌다"고 적었는데 그건 틀렸다.**
> 승인된 도메인 제한은 OAuth 팝업·리디렉션 방식에만 걸리고 익명 로그인에는 적용되지 않는다.
> 콘솔에서 도메인을 건드릴 필요가 없다.

실제로 막히는 것은 규칙 두 가지뿐이다. 배포본에서 직접 찔러 확인했다.

```
hostUpdate({round: 12})        → PERMISSION_DENIED   (기존 규칙 상한이 7)
hostUpdate({board: "..."})     → PERMISSION_DENIED   ($other:false 가 미등록 필드를 거부)
```

**선생님이 하실 일은 하나다.** Firebase 콘솔 → Realtime Database → 규칙에
이 저장소의 `경제수학/live/firebase-database.rules.json` 을 붙여넣고 게시한다.
그러면 세계시장 친구방이 바로 열린다.

### 규칙에서 바꾼 것

- `round` 상한 `<= 7` → `<= 30`. 세계시장은 12라운드라 기존 상한에 걸린다.
- `board` 필드 추가(호스트만 쓰기, 문자열 30,000자 이하). `$other: {".validate": false}` 때문에
  규칙에 없는 필드는 서버가 거부한다. 판 스냅샷을 올리려면 반드시 필요하다.

### 라이브 구조 — 라운드당 제출 1회

기존 인프라가 "동시 제출 → 호스트 정산 → 동시 공개" 모델이라 거기에 맞췄다.

> 호스트가 전원 주사위를 굴려 이동 → 각자 도착 칸과 환율을 보고 **동시에** 결정 제출
> → 호스트 정산(환전·매수·개발·통행료·세금) → 사건 → 다음 라운드

순서를 기다리는 시간이 없다. 부루마불을 그대로 옮겼을 때의 최대 실패 요인을 피한 것이다.

- 판 전체를 JSON 문자열로 압축해 `economyRooms/{code}/board` 에 올린다. 3인 기준 **848바이트**.
- 참가자는 스냅샷을 복원해 기존 `render()` 를 그대로 쓴다. 렌더 코드를 두 벌 만들지 않았다.
- **공용 `realtime-adapter.js` 는 건드리지 않았다.** 다른 게임 8개가 같이 쓰는 파일이라 위험하다.
  대신 제출 내용을 `strategyId` 문자열에 인코딩했다 — `b12_500~buy` = 12번 통화 500만원어치 매수 +
  도착 도시 매수. 규칙의 `strategyId` 30자 제한 안에 들어간다.

### 밟은 함정 두 가지

- **호스트가 자기 스냅샷을 되읽어 자기 상태를 파괴했다.** `hostUpdate` → 자기 `watchRoom` 콜백 →
  `snapIn` 으로 S 교체 → 시드 난수와 사건 순서가 날아가 다음 라운드가 딴 판이 된다.
  호스트는 스냅샷을 되읽지 않도록 막았다. **호스트의 S 가 원본이다.**
- **`localStorage` 를 감싸지 않아 입장이 통째로 죽었다.** 시크릿 모드나 사이트 데이터 차단이면
  접근 자체가 예외를 던진다. 이름 저장/복원을 try/catch 로 감쌌다.

### 검증한 것

- 가짜 실시간 어댑터(메모리)로 3인 4라운드 전체 라이브 게임 — 콘솔 오류 0, 최종 순위 렌더까지
- 정산 검증: 안 가진 통화에 대한 "팔기" 제출이 무시됨, 통행료·매수·환전이 모두 반영됨
- 중복 제출이 와도 `LIVE.phase==='act'` 가드로 정산이 한 번만 실행됨

### 검증하지 못한 것

**실제 Firebase 다기기 플레이는 확인할 수 없었다.** 위 도메인 문제 때문이기도 하고,
개발 미리보기가 `data:` URL 이라 상대경로 스크립트와 인증 오리진이 모두 성립하지 않는다.
**폰 두 대로 방을 만들고 들어가는 것까지는 선생님이 직접 해보셔야 한다.**

### 사이트 연결

- `경제수학/index.html` 에 네 번째 경로 카드(보드게임) 추가, 상단 내비에도 링크 추가.
- `경제수학_허브_flow.css` 의 `.route-grid` 를 3열 → 4열로, 900px 이하는 2x2 로.
  이 CSS 를 읽는 HTML 은 `경제수학/index.html` 하나뿐이라 `?v=3` → `?v=4` 한 번만 올리면 된다(함정 5).


## 2026-09-09 (3) · 환전 도입 — 밸런스가 뒤집혔다

앞 항목에서 찾은 구조 문제("많이 사기가 잘 골라 사기를 이긴다")를 2번 안(환전 행동 추가)으로 풀었다.

- **매 턴 주사위를 굴리기 전에 환전할 수 있다.** 세계은행 칸에만 두면 한 판에 한 번뿐이라
  실력이 될 수 없어서, 턴 시작 화면에 `💱 환전소` 버튼을 붙였다(12라운드면 12번의 판단).
- 외화를 원하는 만큼 사서 들고 있다가 오르면 되판다. 도시는 밟아야 살 수 있어 물량이 실력을
  이기지만, **외화는 언제든 원하는 만큼 살 수 있어 환율을 읽는 판단이 곧바로 돈이 된다.**
- **살 때와 팔 때 환율에 1%씩 차이(`SPREAD`)를 뒀다.** 왕복 2%라 아무 때나 사고팔면 손해다.
  은행 고시환율에 실제로 있는 것이고, 사자마자 총재산이 2% 줄어 보이는 것이 맞다 —
  수수료를 설명하는 것보다 눈으로 보게 하는 편이 낫다.
- 보유 외화는 **지금 팔면 받을 돈**으로 총재산에 넣는다(`fxValue`).
- NPC에도 `fx()` 판단을 붙였다. `환율 사냥꾼`은 -6% 아래로 내려간 통화를 현금의 45%로 사서
  +5%에 되판다. `안전 제일`은 안전자산·선진국 통화만 -9% 아래에서 30%로. 나머지 셋은 환전하지 않는다.

### 결과 — 의도한 교훈이 성립한다

NPC 50판 자동 대전(12라운드, 사람은 환전 안 하는 중립).

| 전략 | 평균 순위 | 우승 | 평균 도시 | 평균 재산 |
|---|---|---|---|---|
| **환율 사냥꾼** | **1.68위** | **29/50** | 1.5곳 | 3,340만 |
| 묻지마 투자 | 2.48위 | 12/50 | 4.2곳 | 3,137만 |
| 안전 제일 | 2.86위 | 6/50 | 1.2곳 | 2,980만 |
| 복리 신봉자 | 3.96위 | 2/50 | 0.6곳 | 2,710만 |
| 개발왕 | 4.02위 | 1/50 | 1.8곳 | 2,720만 |

**도시를 가장 적게 가진 전략(1.5곳)이 가장 많이 가진 전략(4.2곳)을 이긴다.**
직전 측정에서 묻지마 투자가 27/50승이던 것이 12승으로 내려왔다.
재산 차이는 1등과 2등이 6.5%라 다른 전략도 죽지 않았다.

### 검증한 것

- 환전 왕복 검산: 500만 → 5,824,112 나이라 (5,000,000 / (0.85 x 1.01)), 환율 +12% 뒤 표시 손익 +9.8%(수수료 2% 차감), 되팔아 2,549만 회수
- 산 직후 총재산이 정확히 2% 줄어드는 것 확인 (왕복 수수료)
- 6인(사람1+NPC5) 12라운드 전체 자동 플레이 — 콘솔 오류 0, 결과 화면 정상
- 모바일 375px 환전소 렌더 확인. `.fx-mkt .row` 가 4열인데 요소가 5개라 `사기` 버튼이
  세로로 찌그러지던 것 수정(열 5개로)

### 다음

- 아직 사이트 내비게이션에 연결하지 않았다(앞 항목과 동일한 이유).
- 친구방 LIVE 이식은 그다음. 환전은 동시 제출 구조와 잘 맞는다 — 이동은 순차,
  환전과 매수 결정은 전원 동시로 두면 된다.
- 교실에서 한 판 돌려보고 환전소가 학생에게 너무 복잡하지 않은지 확인이 필요하다.


## 2026-09-09 (2) · 세계시장 원정에 NPC 추가 — 그리고 밸런스 문제를 찾음

혼자 하고 싶은 학생을 위해 NPC를 넣었다. 사람 1~6명 + NPC 0~5명, 합계 2~6명.

- NPC 5종은 각각 다른 경제 전략을 쓴다: `환율 사냥꾼`(싸진 통화만) · `묻지마 투자`(무조건 매수)
  · `안전 제일`(안전자산·선진국만) · `개발왕`(싼 도시 사서 개발) · `복리 신봉자`(은행 우선).
- **NPC는 판단할 때마다 이유를 한 줄 말한다.** 혼자 하는 학생에게는 그 한 줄이 유일한 교사다.
  한 판(12라운드)에 약 56번 나온다.
- 한국어 조사 헬퍼(`GA`/`NEUN`)를 넣었다. 통화 이름이 매번 달라서 "셰켈가"처럼 틀리던 것을
  받침으로 판정해 "셰켈이"로 고친다.

### NPC 토너먼트가 드러낸 것 — 게임이 거꾸로 가르치고 있었다

NPC끼리 40~50판씩 자동으로 붙여 평균 순위를 쟀다. 사람 플레이테스트로는 몇 주 걸릴 검증이다.

**1차 결과: 의도한 정답 전략(`환율 사냥꾼`)이 5명 중 4등.** 원인 두 가지를 찾아 고쳤다.

- **사건표에 편향이 있었다.** 태그별 합을 재니 산유국 **+35**, 원자재 +19는 늘 오르고
  농업 -20, 몬순 -17은 늘 내렸다. 환율을 볼 필요 없이 태그만 외우면 이기는 구조였다.
  → 균형용 사건 7개를 추가해 모든 태그 합을 **-1 ~ +2** 로 맞췄다(사건 18 → 25개).
- **환율이 순수 랜덤워크였다.** 비싸게 사도 그 도시의 원화 가치가 같이 높아서 손해가 아니었다.
  → 평균 회귀(`REVERT=0.13`)를 넣었다. 구매력 평가 되돌림과 같은 방향이고,
  이게 있어야 "쌀 때 사서 회복될 때 이득"이 성립한다.
- 은행이 너무 셌다(라운드당 7% 복리 = 12라운드에 2.25배, 무위험). `DEPOSIT_RATE` 0.07 → **0.03**.

**2차 결과: 그래도 `묻지마 투자`가 1위(27/50승).** 레버를 네 개 더 돌렸지만 결론이 같았다.

| 시도 | 결과 |
|---|---|
| 사건표 균형 + 평균 회귀 | 묻지마 14승 → 5승, 대신 복리 신봉자 17승 |
| 은행 이자 0.07 → 0.03 | 복리 신봉자 죽고 묻지마 27/50승 |
| 시작 자금 2500 → 600 | 묻지마 16/30승 (도시 3.6 vs 사냥꾼 1.8) |
| 현금에 이자 4% (기다리는 손해 제거) | 묻지마 11승, 사냥꾼은 오히려 3.3위로 악화 |

**구조적인 문제다. 부루마불 형태에서는 "많이 사기"가 "잘 골라 사기"를 이긴다.**
통행료 수입은 보유 도시 수에 비례해 게임 내내 쌓이는데(도시당 약 55%),
환율 타이밍 이득은 도시당 한 번 7% 남짓이다. 자릿수가 다르다.

### 그래서 다음에 정해야 할 것

두 갈래다. 선생님 결정이 필요하다.

1. **유지비 도입(작은 수정)** — 라운드마다 보유 도시 가치의 약 3%를 유지비로 낸다.
   통행료 수익률(6인 기준 라운드당 약 4.6%)에서 유지비를 빼면 순수익이 1.6%로 줄어
   물량의 우위가 크게 꺾인다. 재산세·관리비라는 뜻이라 경제적으로도 말이 된다.
2. **환전 행동 추가(구조 수정)** — 도시와 별개로 외화를 직접 사고팔 수 있게 한다.
   쌀 때 엔을 사서 회복되면 되판다. 도시 물량 경쟁과 겹치지 않는 순수 타이밍 판이라
   환율 판단이 곧바로 돈이 되고, 2022 개정 '수와 경제'의 환율 단원과 정확히 맞는다.

어느 쪽도 하지 않는다면, **"환율을 읽으면 이긴다"고 학생에게 말하면 안 된다.**
지금 상태에서 환율은 매 턴 실제 환산 계산을 시키는 훌륭한 장치이지만, 승패를 가르는 축은 아니다.


## 2026-09-09 · 경제수학 보드게임 첫 프로토타입 (세계시장 원정)

- `경제수학/경제수학_세계시장.html` 신규. 2~6인 한 기기 pass-and-play. 자체완결(인라인 CSS/JS)이라 공용 파일과 `?v=` 를 건드리지 않는다.
- 부루마불의 형태만 빌리고 엔진은 환율로 굴린다. 도시 값은 **현지 통화로 고정**하고 원화 환산값만 환율로 움직인다. 통화가 약할 때 사서 회복되면 이득 — 이게 승패의 주된 축이다.
- 나라 35개 풀에서 판마다 16개를 뽑아 말판을 만든다. 화폐 이름·코드 실제 값 사용. 기준 환율은 실제 시세 근사값이며 가끔 갱신하면 된다.
- **실시간 환율 API 는 쓰지 않기로 했다.** 환율은 하루 0.1~0.5% 라 15분 게임에서 안 움직이고, 수업 중 API 장애가 곧 수업 중단이며, 학생마다 다른 값을 보게 된다. 실제 값은 출발점으로만 쓰고 게임 중 변동은 사건이 만든다.
- **사건은 실제 국가를 지목하지 않는다.** 나라에 태그(지진대·몬순·산유국·관광의존·원자재·안전자산·신흥국·제조업·농업·선진국)를 달고 사건은 태그만 때린다. 교실 안전 문제를 피하면서, 학생이 "어느 나라가 그 속성이지"를 추론하게 되어 오히려 낫다.
- 8인 확장은 하지 않기로 했다(선생님 결정). 2~6인 유지.

### 측정해서 정한 것 — 기본 라운드는 12

4인 greedy 자동 플레이 6판씩 돌려 1등·꼴찌 재산 격차를 쟀다.

| 라운드 | 평균 격차 | 판별 분포 |
|---|---|---|
| 8 | 17% | 5 · 9 · 12 · 13 · 28 · 33% |
| 12 | 42% | 26 · 29 · 38 · 43 · 43 · 72% |
| 16 | 44% | 16 · 30 · 37 · 39 · 56 · 85% |

8라운드는 격차가 5%까지 떨어지는 판이 나와 누가 이겼는지 체감이 안 된다. 16은 12보다 나아지는 것 없이 시간만 는다. **기본값 12.**
초기 8라운드 판이 밋밋했던 진짜 원인은 출발선 보너스였다. 전원에게 똑같이 주는 돈이 커서(2바퀴반 = 700만) 판단 차이를 덮었다. `PASS_BONUS` 300→150, `TOLL` 0.16→0.22 로 조정했다.

### 검증한 것

- 4인 8라운드 실제 클릭 전체 플레이, 6인 12라운드 자동 플레이 — 콘솔 오류 0
- 은행 복리 표기 검산: 600만 x 1.07^8 = 1,031만 (화면값 일치)
- 사건 16회 누적 후 환율 스프레드 +42% ~ -30% — 폭주 없음
- 모바일 375px 레이아웃, 사건 오버레이, 결과 화면 렌더 확인

### 남은 것

- **아직 사이트 내비게이션에 연결하지 않았다.** 교실에서 한 판 돌려보고 규칙이 재미있는지 확인한 뒤 붙이는 게 안전하다. `경제수학/index.html` 의 `.route-grid` 는 `repeat(3,1fr)` 이라 4번째 카드를 넣으려면 `경제수학_허브_flow.css` 를 고쳐야 하고, 그러면 함정 5(`?v=` 일괄 상향)가 걸린다.
- 친구방 LIVE 이식은 그다음. 기존 동시제출 구조에 맞추려면 이동은 순차, 매수·개발 결정은 전원 동시로 나눠야 한다.
- Firebase 는 문서 기준 아직 콘솔에서 활성화 전이다. 진짜 다인 플레이 전에 확인 필요.
- Windows 크롬은 국기 이모지를 두 글자 코드로 표시한다(PE, CZ...). 폰에서는 국기로 보인다. 고치려면 SVG 를 넣어야 해서 두지 않았다.


## 2026-08-31 · 도전하기 정리와 계산 스킬 Boss Battle 2.0 첫 적용

- 전역 `도전하기`를 `오늘의 도전 → 친구방 LIVE → 보스전` 세 갈래로 정리했다.
- 새 `보스전/` 모음 페이지를 만들고 첫 실전 보스로 `다항함수 빠른 미분 · 미분의 철갑수`를 연결했다.
- 45초·HP 1,800·데미지·콤보·PHASE 2 철갑 방어·즉시 재도전 구조를 적용했다.
- 다른 계산 스킬 27개의 기존 보스는 학생 반응을 보기 전까지 유지한다.
- 경제수학 LIVE 아래에 중복 노출되던 `ECONOMY LIVE MAP` 8게임 지도를 제거했다.
- 다음 작업은 기하 아레나의 첫 공간 조작형 프로토타입이다. 공통 8문제 껍데기를 먼저 고치지 말고, 공간 위치관계를 `관찰 → 회전/이동 → 판단 → 즉시 피드백`으로 체험하는 게임 하나를 만든다.

## 2026-08-30 · 미적분 게임별 적용 시작

- `오늘의 미적분` 다음 대상으로 `극값 사냥`을 선택했다.
- 기존 게임 규칙은 보존하고 이름 공유, 개인 최고, 정확도, 즉시 재도전, `calculus-peak` 행동 로그를 연결했다.
- 다음 작업은 같은 페이지의 `부호 변화 판독`을 실제로 끝까지 플레이하며 문제 다양성과 중근 피드백을 점검하는 것이다.

### 이어서 완료한 범위

- `부호 변화 판독`의 중근·부호 변화 판정과 모바일 수식 확인
- `삼차함수 개형전`의 두 게임에 이름·개인 최고·정확도·즉시 재도전·행동 로그 연결
- `사차함수 연결전`의 두 게임에 같은 플레이 루프 연결
- 다음 미적분 작업은 메인 아케이드 밖의 `롤러코스터`와 계산 스킬 모드의 우선순위를 정하는 것
- 그다음 과목 순서는 기하 → 경제수학 → 수학 주제 마이닝

## 2026-08-31 · 기하 공통 아레나 적용

- `기하_공통.js`의 `GeoLab.Arena`에 공통 플레이어 이름·정확도·개인 최고·즉시 재도전·익명 행동 로그를 연결했다.
- 공통 아레나를 쓰는 기하 단원 전체에 자동으로 적용된다. 개별 공간 그림과 문제 생성기는 수정하지 않았다.
- 대표 게임 `스페이스 오비트`를 모바일에서 결과까지 플레이하고 한 번 터치 재도전을 확인했다.
- 공통 생성형 페이지 `기하_타원.html#game`에서도 이름 공유와 시작 화면을 확인했다.
- 다음 작업은 경제수학 8게임의 현재 공통 런타임과 결과 구조를 점검하는 것이다.

---

세션이 끊기거나 사용량이 차서 새로 시작할 때, 이 문서만 읽고 바로 이어갈 수 있게 적어 둔다.
마지막 갱신: 2026-08-30 · 기준 커밋 `6c4ff1b`

---

## 0. 먼저 읽을 것 — 함정 다섯 가지

지난 세션에서 실제로 시간을 버린 것들이다. 같은 데서 또 넘어지지 말 것.

### 함정 1. 기하 단원 페이지는 스크립트를 **두 개** 읽는다

```
기하_구의방정식.html
  → 기하_학습.js        (페이지 뼈대 + 범용 실험실)
  → 기하_구_direct.js   (실험실을 통째로 덮어씀)  ← 학생이 보는 건 이쪽
```

`_direct.js` 는 `[data-page="lab"]` 안의 제목·설명을 바꾸고 `#genericLabSvg` 를
`cloneNode` 로 갈아 끼운다. **그래서 `기하_학습.js` 쪽 실험실을 고쳐 봐야 화면에 안 나온다.**

direct 스크립트가 있는 페이지(8개):
공간좌표와내분점 · 구의방정식 · 벡터로표현한직선 · 벡터로표현한평면과구 ·
벡터와연산 · 벡터의내적 · 위치벡터와성분 · 정사영

없는 페이지(4개, 여기는 `기하_학습.js` 가 진짜 실험실):
삼수선정리 · 쌍곡선 · 이차곡선의접선 · 타원

### 함정 2. 백슬래시가 전달 중에 하나로 줄어든다

파일에 `\\sqrt` 를 넣어야 JS 가 `\sqrt` 를 만든다. 그런데 스크립트로 파일을 쓸 때
`\\\\` 가 `\\` 로 줄어드는 일이 반복됐다. **백슬래시는 `chr(92)` 로 만들어 붙일 것.**
넣은 뒤에는 반드시 `node -e "...eval..."` 로 런타임 값을 확인한다.

### 함정 3. 브라우저 창이 숨겨져 있으면 스크린샷이 백지로 나온다

`tabs_context` 가 "Browser pane is currently hidden" 이라고 하면 스크린샷은 의미가 없다.
그럴 땐 DOM/좌표를 직접 재서 검증한다(폴리곤 좌표, viewBox 안에 들어가는지, 계산값 일치).
**색감·여백처럼 눈으로 봐야 하는 건 선생님께 확인을 요청할 것.**

### 함정 4. 이 브라우저 창에서는 requestAnimationFrame 이 멈춘다

`document.hidden` 이 true 라서 rAF 가 **600ms 에 1회**만 불린다(setInterval 은 같은 시간에 38회).
애니메이션을 rAF 로만 돌리면 "출발을 눌렀는데 아무 일도 안 일어남" 으로 보인다.
경제수학 결과 화면, 롤러코스터 주행에서 두 번 겪었다.
**움직이는 것은 setInterval 로 돌리거나, rAF 를 쓰면 타이머 안전장치를 함께 걸 것.**

### 함정 5. 버전 올리기를 빼먹으면 캐시된 옛 파일이 온다

JS/CSS 를 고치면 그걸 읽는 **모든** HTML 의 `?v=` 를 올린다.
특히 공용 파일(`game-runtime.js`, `기하_공통.js`)은 여러 페이지가 읽는다.
`grep -rn "파일명" --include=*.html .` 로 참조를 전부 찾고 올린다.
이번에도 걸렸다. 고쳐 놓고 화면이 그대로여서 코드를 의심했는데, 캐시된 옛 파일이었다.
**화면이 안 바뀌면 코드를 다시 보기 전에 버전부터 확인할 것.**

---

## 1. 지금까지 끝난 것

| 작업 | 커밋 | 상태 |
|---|---|---|
| 주제탐구 운영실 실시간 연동 | `9a69373` | 배포 완료 |
| 경제수학 8라운드 누적 스카이라인 | `8b5dfdc` | 배포 완료 |
| 기하 구의 방정식 — 돔으로 보기 | `b47f4ab` | 배포 완료 |
| 돔 밑면이 구에 가려지던 것 수정 | `351f7e1` | 배포 완료 |
| 기하 정사영 — 바닥 전환 + 경사 지붕 | `9ccb330` | 배포 완료 |
| 미적분1 롤러코스터 설계 | `f6a3eae` | 배포 완료 |
| 롤러코스터 전역 내비 누락 수정 | `517c127` | 배포 완료 |
| 미적분1 허브 — 적분 9개 연결 + 단원 묶기 | `eeb931d` | 배포 완료 |

---

## 2. 사이트 길찾기 정비 — 대부분 끝남

전체 링크를 훑어 진단한 결과. 진단서: 아티팩트 "입구가 여러 개인 방".

### 끝난 것
- 적분 9개 되찾기 + 미적분 허브를 Ⅰ·Ⅱ·Ⅲ 단원으로 묶기 (`eeb931d`)
- 개발용 점검 파일 4개를 저장소에서 내림. 로컬에는 남아 있음 (`eeda765`)
- 메인화면 7.2화면 → 1화면. 곡선 애니메이션은 남기고 높이만 절반 (`2ca51e6`)
- 밝은 카드 목록이 밋밋해서 어두운 무대로 되돌림 (`d8c6e3b`)
- "친구방 LIVE"가 두 곳을 가리키던 것을 하나로 (`c5e080e`)
- 모바일에서 히어로 그림이 잘려 보이던 것 (`47b830f`)
- 카드 그림에 살 붙이고 제목 키움 (`6c4ff1b`)

### 배운 것 — 다음에도 쓸 것
- **참고 사이트가 언급되면 실제로 열어 볼 것.** 이름만 듣고 짐작으로 만들었더니
  "얍실하다"는 말을 들었다. 21st.dev·Awwwards 를 열어 보니 제목 크기와
  카드의 내용 밀도가 확연히 달랐다.
- **한 화면으로 줄일 때 위치까지 같이 봐야 한다.** 모바일 히어로 그림은 원래
  화면 밖으로 흘려보내는 배경인데, 크기만 줄이고 위치를 두었더니 잘린 조각이 됐다.
- **아이소메트릭은 넓고 낮은 칸에 맞지 않는다.** 세로로 넘쳐 잘린다.
  그럴 땐 정면 구도로 바꾼다.

### 남은 것
- **미적분 카드 그림**: 지금은 곡선·접선·넓이. 선생님이 "그대로 두자"고 하셨다.
  나중에 바꾼다면 후보는 **우리 롤러코스터 트랙을 그대로 그리고 접선을 얹는 것** —
  실물이면서 수학인 유일한 지점이고 롤러코스터 실험실과도 이어진다.
  (사진은 쓰지 않기로 했다. 롤러코스터 사진은 미적분이 아니라 놀이공원으로 읽힌다)
- **jp-home-2.js 정리**: calculus/space/economy 데모가 홈에서 더 이상 쓰이지 않는다.
  가드가 있어 조용히 건너뛰므로 급하지 않다.
- **이름이 갈리는 다른 곳**: 주제탐구 홈(문 7 · 이름 7), 홈으로 가는 길(이름 5).
  친구방 LIVE 만큼 심각하지는 않다.

---

## 3. 다음 작업 — 수학 영상 (신비한 건축사전 룩)

### 목표

수학의 원리·역사·"왜 그렇게 됐는가"를 1분 안팎으로. 예: *아리스토텔레스·아르키메데스는 어떻게 적분했을까*.
**흥미가 먼저고 학습은 그 다음**이라는 게 출발점이다.

### 왜 AI 영상 생성을 쓰지 않는가

핵심 장면이 **정확한 작도**다. "원에 내접·외접하는 96각형으로 원주율을 좁혀 간다" 같은 건
AI 영상이 뭉갠다. 돈을 써도 결과가 안 나오는 쪽이다.
(Grok · Higgsfield · Seedance 모두 유료이고, 이 용도에는 맞지 않는다)

### 대신 0원 파이프라인

| 단계 | 방법 | 비용 |
|---|---|---|
| 대본·구성 | Claude | 0 |
| 수학 장면 | **사이트가 이미 렌더러다.** 랩/게임에 카메라 경로 + 타임라인을 얹어 프레임을 뽑는다 | 0 |
| 사료 이미지 | 퍼블릭 도메인 (아르키메데스 팔림프세스트, 1544 초판, 옛 판화, Wikimedia) — 역사물은 **진품이 AI 가짜보다 낫다** | 0 |
| 프레임 → 영상 | ffmpeg | 0 (설치 필요) |
| 내레이션 | **선생님 목소리 권장** — 학생이 아는 목소리면 몰입도가 다르다 | 0 |

### 룩은 렌더링 품질이 아니라 연출에서 나온다

Blender 포토리얼은 브라우저에서 안 나온다. 흉내내려다 어설퍼지느니 이렇게 간다.
- 어두운 배경 + 강조색 하나
- 느린 카메라 이동 (등속보다 ease-out)
- 숫자가 결정적으로 한 번 크게 등장
- 여백을 많이 둔 타이포

### 현재 환경

Python 3.12 · node 24 · numpy · pillow **있음** / ffmpeg · manim **없음**

---

## 4. 잊지 말 것 — 보안 경계

- `주제탐구/data-private/` 의 학생 실명 자료는 **로컬 전용**, 공개 저장소에 절대 올리지 않는다
- 교사용 열쇠(토큰)는 대화·커밋·공개 페이지에 **절대** 남기지 않는다. 브라우저 localStorage 에만 둔다
- 운영실 접속 비밀번호는 Claude 가 입력하지 않는다

---

## 5. 현재 장기 작업 — 첫 실사용 플레이 루프

2026-08-30 기준. 상세 감사와 데이터 구조는 `JP_MATH_LAB_FIRST_PLAYABLE_PROTOTYPE.md`에 있다.

### 범위

- 전체 리뉴얼이 아니라 기존 게임 하나만 실사용 수준으로 만든다.
- 선정: `미적분1/오늘의_미적분.html`
- 흐름: 플레이 이름 → 오늘의 함수 홈 → 5개 미션 → 결과 → Personal Best → 바로 다시하기.
- Seed 대량 구축, 캐릭터, Gold, 카드, 전체 랭킹, Teacher Studio, Boss Engine 일반화는 하지 않는다.

### 구현 상태

- 플레이 이름과 로컬 userId 생성.
- 시작 시 미완료 로그, 결과 도착 시 완료 로그 기록.
- `userId`, `gameId`, 시작·종료시간, 점수, 정확도, 플레이시간, retry, 세션 내 순번, Personal Best 저장.
- 결과 첫 화면을 Score, Accuracy, Personal Best, 한 줄 피드백, Retry 중심으로 단순화.
- 기존 함수 읽기 지도와 추천 스킬은 `이번 판 자세히 보기` 안에 보존.
- 모바일 390×844와 데스크톱 1440×900에서 실제 플레이 검증.
- `바로 다시하기` 한 번으로 MISSION 1 / 5와 점수 0이 시작되는 것 확인.
- 자동 테스트: `node tests\first-playable-prototype.test.js`.

### 다음에 확인할 것

- `경제수학/live/firebase-database.rules.json`의 `gameTelemetry` 규칙을 Firebase Console에 게시해야 중앙 익명 로그가 활성화된다.
- 규칙 게시 전에도 게임과 로컬 로그는 정상 동작한다.
- GitHub Pages 배포 뒤 공개 주소에서 모바일 플레이를 한 번 더 점검한다.
- 실제 학생 테스트에서는 완료율, 즉시 재도전율, 평균 플레이 횟수, PB 갱신율만 먼저 본다.

### 2026-08-31 확장 상태

- 미적분 메인 아케이드 6게임: 개인 최고·정확도·즉시 재도전·행동 기록 연결 완료
- 기하 공통 경기장 16개: 같은 플레이 루프 연결 완료
- 경제수학 8게임: 8라운드·가변 사건·가변 수치·가변 발문·직접 비율 배분·개인 최고·재경기 연결 완료
- 경제수학 실시간 친구방: 기존 Firebase 데이터 구조를 유지하며 개인별 행동 기록과 재경기 자동 감지 연결 완료
- 다음 작업: `JP_MATH_LAB_수학_주제_마이닝_1차.md`의 상위 후보를 검증하고 Seed Registry로 승격
