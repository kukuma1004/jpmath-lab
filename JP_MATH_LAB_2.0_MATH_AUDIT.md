# JP Math Lab 2.0 · Math Rendering Audit

> 점검일: 2026-08-25
> 목적: 수식을 페이지별 임시 스타일이 아니라 공통 학습 데이터로 안정적으로 표시

## 1. 점검 결과

- 추적 중인 HTML: 69개
- 공통 JP 화면 레이어 사용: 65개
- 수식 가능성이 있는 화면: 32개
- 동적 문제 생성과 수식이 함께 있는 화면: 23개
- 기존에 공통 수식 렌더러를 직접 불러오던 화면: 11개

가장 큰 위험은 수식 엔진의 부재보다 **페이지마다 다른 시점과 방식으로 수식을 조판하는 것**이었다. 특히 `innerHTML`로 새 문제를 만든 뒤 다시 렌더링하지 않거나, 일반 UI용 `span` 스타일이 KaTeX 내부 구조에 적용되면 분수·극한·위첨자가 깨질 수 있었다.

## 2. 적용한 공통 구조

```text
jp-polish.js
  ├─ 현재 경로와 화면 성격 판별
  ├─ KaTeX가 없을 때만 로컬 엔진 준비
  └─ jp-math-render.js 연결
       ├─ 초기 수식 조판
       ├─ 동적으로 추가된 문제 재조판
       ├─ 모바일 폭에 맞춘 최소 축소
       └─ 오류·미표시·오버플로 진단
```

공통 대상에는 다음 요소가 포함된다.

- 개념의 핵심 공식
- 예제·문제·질문 수식
- 60초 러시와 보스전 문제
- 수식형 선택지
- 미적분·기하 계산 스킬 공식
- 명시적인 `data-math`, `data-tex` 요소

한글만 있는 선택지처럼 수식이 아닌 요소는 자동 조판하지 않는다.

## 3. 오류 처리 원칙

- 수식 원문을 `data-math-source`에 보존한다.
- 성공하면 `data-math-rendered="true"`를 표시한다.
- 실패하면 `data-math-error="true"`와 개발 콘솔 경고를 남긴다.
- 렌더 실패를 빈 화면으로 숨기지 않는다.
- `JPMath.audit()`는 렌더 수, 오류, 미처리 수식, 가로 넘침을 반환한다.

## 4. 실제 검증

### 대표 상호작용 검증

- 미적분 계산 스킬 `0/0꼴 인수분해`
- 기하 내신 스킬 `타원 a·b·c 계산`
- 기하 보스전의 타원 표준형과 네 개 선택지
- 오늘의 미적분의 고정 함수와 극한 분수식

기하 보스전은 데스크톱과 폭 390px 모바일에서 직접 시작해 수식과 선택지를 확인했다.

### 자동 순회 검증

수식 가능성이 있는 32개 화면을 로컬 사이트에서 순서대로 열어 검사했다.

- 수식 렌더 오류: 0
- 처리 대기 수식: 0
- 페이지 가로 넘침: 0
- KaTeX 수식이 실제 확인된 화면: 22개

나머지 화면은 현재 선택·진입 상태에서 수식 요소가 없거나, 수식이 게임 시작 이후 생성되는 화면이다.

## 5. 앞으로 지킬 규칙

1. 새 문제 HTML을 만든 뒤 페이지별 KaTeX 코드를 새로 작성하지 않는다.
2. 수식 요소에는 기존 공통 클래스 또는 `data-math`·`data-tex`를 사용한다.
3. 일반 UI의 자식 선택자는 가능한 한 `>`를 사용해 KaTeX 내부까지 침범하지 않게 한다.
4. 배포 전 분수·극한·적분·벡터·위첨자를 포함한 대표식을 폭 390px에서 확인한다.
5. 보스전과 LIVE처럼 실행 후 나타나는 수식은 반드시 실제 시작 단계까지 눌러 검사한다.
