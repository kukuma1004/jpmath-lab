# JP Math Lab 첫 실사용 프로토타입

## 1. 이번 작업의 범위

이번 버전의 성공 기준은 기능 수가 아니다.

> 학생이 한 판을 끝낸 직후 `바로 다시하기`를 눌러 두 번째 판을 시작하는가?

v13의 Seed DB, 캐릭터, Gold, 카드, 전체 랭킹, Teacher Studio, Boss Engine 일반화는 이번 범위에서 제외한다. 기존 게임 하나의 플레이 루프와 측정 가능한 행동 로그만 완성한다.

선정 게임은 **미적분Ⅰ의 `오늘의 미적분`**이다.

```text
PLAY LOGIN
→ HOME(오늘의 함수 소개)
→ PLAY(5개 미션)
→ RESULT
→ PERSONAL BEST
→ 바로 다시하기
```

---

## 2. 현재 코드 감사

### 2.1 현재 게임 목록

현재 저장소는 빌드 도구가 없는 정적 HTML/CSS/JavaScript 프로젝트다. 게임은 한 가지 엔진으로 통일되어 있지 않고 다음 계열로 나뉜다.

| 계열 | 현재 콘텐츠 | 공통 구조 |
|---|---|---|
| 미적분 DAILY | 오늘의 미적분 | 날짜별 함수 1개, 5개 미션, 1,000점 만점 |
| 미적분 ARCADE | 극값 사냥, 삼차함수 개형전, 사차함수 연결전 | 8문제, 제한시간, 라이프, 콤보, 로컬 TOP 5 |
| 미적분 계산 스킬 | 28개 스킬 | 원리, 5문제, 60초 러시, 제한시간 보스 |
| 미적분 개념 페이지 게임 | 극한·연속·미분가능성·접선·평균값정리 등 | 페이지 내부에 독립 미니게임과 로컬 최고기록이 혼재 |
| 기하 개념 아레나 | 14개 개념 LAB의 게임 탭 | 공통 `Arena`, 8초, 3라이프, 콤보, 솔로/로컬 1:1 |
| 기하 내신 스킬 | 36개 스킬 | 원리, 5문제, 60초 러시, 3연속 보스 |
| 경제수학 | 투자왕 + 은행 CEO + 내 집 마련 + 환율 전쟁 + 인플레이션 생존 + 스타트업 CEO + 펀드매니저 + 경제정책 시뮬레이션 | 8라운드, 사건·수치·발문 변형, 전략 비율 배분, 솔로/친구방 |

### 2.2 게임 구조

- `오늘의 미적분`: 독립 HTML/CSS/JS다. 다른 게임을 수정하지 않고 실험하기 쉽다.
- 미적분 계산 스킬: `미적분1_계산스킬.js` 한 파일이 28개 스킬의 드릴·러시·보스를 함께 처리한다.
- 기하 개념 게임: `기하_공통.js`의 `Arena`가 여러 개념 페이지에서 공유된다.
- 기하 내신 스킬: `기하_내신스킬.js`가 36개 스킬을 함께 처리한다.
- 경제수학 8게임: `games-catalog.js`의 게임 데이터와 `game-runtime.js`의 공통 계산, `games-app.js`/`room-app.js`의 솔로·실시간 화면으로 구성된다.

### 2.3 점수 계산 방식

- 오늘의 미적분: 미션마다 `정확도 160점 + 속도 40점`, 5개 미션 최대 1,000점.
- 미적분 계산 스킬 러시: 정답 기본 100점에 콤보 보너스를 더하고 오답은 2초를 차감한다.
- 기하 아레나: `100 + 남은 시간 비율 × 100`에 연속 정답 배수를 적용한다.
- 미적분 확장 아레나: 기본점수와 남은 시간을 결합한 뒤 3콤보 단위 배수를 적용한다.
- 경제수학: 전략별 비율과 사건별 payoff의 가중합으로 라운드 변화를 계산한다.

### 2.4 시작과 종료

- 대부분 시작 버튼이 각 파일 내부 상태 객체를 초기화한다.
- 종료 조건은 문제 수 완료, 라이프 소진, 시간 종료, 8라운드 완료 중 하나다.
- 많은 게임이 결과에서 재도전 버튼을 제공하지만 일부는 시작 화면으로 돌아가 모드를 다시 선택해야 한다.
- `오늘의 미적분`은 결과의 재도전 버튼이 이미 같은 함수를 다시 시작하므로 1회 터치 Retry에 가장 가깝다.

### 2.5 저장 방식

- 대부분 `localStorage`에 게임별 서로 다른 키로 최고점·TOP 5·클리어 수를 저장한다.
- 공통 사용자 ID나 공통 플레이 로그는 기존에 없었다.
- 경제수학 친구방만 Firebase 익명 인증과 Realtime Database를 사용한다.
- 학생 기록장 웹앱과 게임 기록은 연결되어 있지 않으며, 이번 버전에서도 연결하지 않는다.

### 2.6 모바일 대응

- 주요 페이지에 모바일 미디어쿼리와 터치 크기가 이미 적용되어 있다.
- `오늘의 미적분`은 모바일에서 1열로 전환되고 선택지는 세로로 쌓인다.
- 기존 결과 화면은 정보가 길게 이어져 Retry 버튼이 늦게 보이는 문제가 있었다.
- 이번 프로토타입에서는 핵심 결과와 Retry를 먼저 보이고 상세 분석을 접었다.

### 2.7 기존 보스전

- 미적분 계산 스킬 보스: 체력 3, 문제별 제한시간, 오답 시 체력 회복, 클리어할수록 제한시간 감소.
- 기하 내신 보스: 3연속 정답, 오답 시 처음부터 재시작.
- 아직 HP·Damage·Combo·Boss Skill·Phase를 모두 가진 공통 Boss Engine은 아니다.
- 이번 프로토타입에서는 보스를 수정하지 않는다.

### 2.8 공통화 가능한 코드

- 사용자 식별, 플레이 시작/완료 로그, Personal Best 판정, Retry 횟수는 게임 종류와 무관하게 공통화할 수 있다.
- 이번에는 `jp-game-telemetry.js`만 작게 추가하고 게임 엔진은 공통화하지 않는다.
- 검증 후 다른 게임이 같은 로그 API를 호출하도록 점진적으로 연결할 수 있다.

### 2.9 다른 게임에 영향을 줄 가능성

- 미적분 계산 스킬·기하 Arena·경제수학 Runtime은 여러 게임이 공유하므로 첫 실험 대상으로 수정 위험이 크다.
- `오늘의 미적분`은 독립 파일이므로 기능 회귀 범위가 가장 작다.
- 새 행동 로그 모듈은 현재 `오늘의 미적분`에서만 불러온다.
- Firebase 규칙 추가는 기존 `economyRooms`와 `economyChoices` 규칙을 변경하지 않고 새 `gameTelemetry` 경로만 더한다.

### 2.10 첫 프로토타입 선정 결론

`오늘의 미적분`을 선택한 이유:

1. 이미 HOME·PLAY·RESULT·BEST·RETRY의 대부분이 존재한다.
2. 점수와 정확도를 동시에 계산한다.
3. 하루 한 함수라는 재방문 이유가 있다.
4. 5개 미션이라 수업 중 테스트 시간이 짧다.
5. 독립 파일이라 다른 게임에 미치는 영향이 가장 작다.
6. 교사가 이전에 모바일 함수 표시와 수식 가독성을 직접 점검한 경험이 있다.

---

## 3. 구현한 첫 플레이 루프

### PLAY LOGIN

- 첫 방문에 플레이 이름을 입력한다.
- 비밀번호·이메일·실명은 받지 않는다.
- 플레이 이름과 로컬 userId는 해당 기기에만 저장한다.
- 온라인 로그에는 플레이 이름을 보내지 않고 Firebase 익명 UID만 기록한다.

### HOME

- 기존 오늘의 함수 소개 화면을 유지한다.
- 현재 플레이어와 오늘 최고 기록을 보여준다.
- 플레이어 칩을 눌러 이름을 바꿀 수 있다.

### PLAY

- 기존 5개 미션, 그래프, 수식, 점수 계산을 그대로 유지한다.
- 시작 순간에 미완료 상태의 로그를 먼저 남긴다.
- 중간 이탈도 시작 수에 포함되므로 완료율을 계산할 수 있다.

### RESULT

처음 보이는 정보는 다음으로 제한한다.

```text
Score
Personal Best
Accuracy
한 줄 피드백
바로 다시하기
```

콤보, 연속 도전, 함수 읽기 지도, 추천 스킬은 삭제하지 않고 `이번 판 자세히 보기` 안에 보존한다.

### RETRY

- `바로 다시하기`는 확인창이나 모드 선택 없이 즉시 첫 미션을 연다.
- 같은 브라우저 세션에서 두 번째 플레이부터 `retry: true`로 기록한다.
- `sessionPlayIndex`는 1, 2, 3 순으로 증가한다.

---

## 4. 행동 로그 구조

한 번의 플레이는 다음 레코드 하나로 저장된다.

```json
{
  "userId": "익명 사용자 ID",
  "gameId": "calculus-daily-function",
  "sessionId": "브라우저 진입 세션 ID",
  "playId": "개별 플레이 ID",
  "startedAt": "ISO 시작시간",
  "endedAt": "ISO 종료시간 또는 빈 문자열",
  "score": 0,
  "accuracy": 0,
  "playTime": 0,
  "retry": false,
  "sessionPlayIndex": 1,
  "personalBest": false,
  "completed": false,
  "dateKey": "YYYY-MM-DD",
  "appVersion": "first-playable-v1"
}
```

- 시작할 때 `completed: false`로 생성한다.
- 결과 화면에 도착하면 같은 레코드를 점수·정확도·시간과 함께 `completed: true`로 갱신한다.
- 로컬 저장은 항상 먼저 수행한다.
- Firebase 전송이 실패하면 `syncState: pending`으로 남겨 다음 전송 때 다시 시도할 수 있다.

### 계산 가능한 지표

| 지표 | 계산 기준 |
|---|---|
| 게임 시작 학생 수 | 고유 `userId` 수 |
| 완료율 | `completed: true` / 전체 시작 레코드 |
| 즉시 재도전율 | `retry: true` 플레이 / 완료 플레이 |
| 학생당 평균 플레이 수 | 전체 플레이 / 고유 userId |
| 세션당 평균 플레이 수 | 전체 플레이 / 고유 sessionId |
| Personal Best 갱신율 | `personalBest: true` / 완료 플레이 |
| 다음날·다음주 재방문 | 같은 userId의 서로 다른 날짜 시작 기록 비교 |

`JPGameTelemetry.summarize()`는 로컬 테스트용 최소 집계를 제공한다. Teacher Studio는 만들지 않았다.

---

## 5. 저장과 개인정보 원칙

- 상세 플레이 이름과 로컬 userId: 학생 기기의 `localStorage`.
- 온라인 행동 로그: Firebase `gameTelemetry/{anonymousUid}/plays/{playId}`.
- 온라인 로그에는 이름, 이메일, 학생코드, 정답 선택 내용이 들어가지 않는다.
- Firebase 규칙은 인증된 익명 사용자가 자기 UID 경로에만 쓸 수 있게 제한한다.
- 일반 사용자는 다른 학생의 로그를 읽을 수 없다.

Firebase 규칙 파일:

```text
경제수학/live/firebase-database.rules.json
```

새 규칙을 Firebase Console에 게시해야 중앙 로그 전송이 활성화된다. 게시 전에도 로컬 플레이와 로컬 행동 로그는 정상 동작한다.

---

## 6. 변경 파일

```text
jp-game-telemetry.js
미적분1/오늘의_미적분.html
미적분1/오늘의_미적분.js
미적분1/오늘의_미적분_프로토타입.css
경제수학/live/firebase-database.rules.json
tests/first-playable-prototype.test.js
```

---

## 7. 검증 기준

자동 검증:

```powershell
node tests\first-playable-prototype.test.js
node --check jp-game-telemetry.js
node --check 미적분1\오늘의_미적분.js
```

브라우저 검증:

1. 모바일에서 플레이 이름 입력.
2. HOME에서 플레이어 이름 확인.
3. 5개 미션 완료.
4. Score, Accuracy, Personal Best 표시 확인.
5. `바로 다시하기` 한 번으로 `MISSION 1 / 5`, Score 0 확인.
6. 데스크톱 가로 넘침 확인.
7. Firebase 규칙 게시 후 중앙 로그의 `synced` 상태 확인.

---

## 8. 실제 학생 테스트 후 다음 판단

최소 1개 학급에서 다음만 본다.

- 시작한 학생 중 몇 명이 끝냈는가?
- 끝낸 학생 중 몇 명이 바로 다시 했는가?
- 두 번째 판의 점수·정확도·시간은 어떻게 달라졌는가?
- 다음날 다시 들어온 학생이 있는가?

즉시 재도전율이 충분하지 않으면 캐릭터나 보스를 추가하기 전에 결과 피드백, 플레이 길이, 문제 변화, 난이도 곡선을 먼저 수정한다.

재미가 확인된 뒤에만 두 번째 게임으로 로그 구조를 확장하고, 그다음 보스 하나를 Boss Battle 2.0 프로토타입으로 만든다.

---

## 9. 미적분 게임별 확장 현황

### 극값 사냥

`미적분1/미적분1_극대극소.html`의 혼자 기록전에 첫 프로토타입과 같은 측정 가능한 재도전 루프를 연결했다.

- 기존 8문제·라이프·콤보·로컬 TOP 5·친구와 1:1은 유지
- `오늘의 미적분`에서 입력한 플레이어 이름을 공유
- 결과 화면에 점수·정확도·개인 최고·세션 내 플레이 순서 표시
- 결과 화면의 기본 행동을 한 번 터치 `바로 다시하기`로 변경
- `calculus-peak` 행동 로그에 시작·완료·재도전·개인 최고 여부 기록
- 모바일 390×844, 데스크톱 1440×900에서 수식·레이아웃·재도전 흐름 확인

같은 페이지의 `부호 변화 판독`은 공통 Arena 화면 개선의 이점을 받지만, 다음 별도 단계에서 문제 다양성·난이도·피드백을 직접 검증한다.

### 미적분 메인 아케이드 확장

메인 화면에 노출된 세 아케이드 페이지의 여섯 게임을 같은 기준으로 연결했다.

- 극값 사냥 (`calculus-peak`)
- 부호 변화 판독 (`calculus-sign`)
- 삼차함수 개형 맞히기 (`calculus-cubicshape`)
- 삼차함수 극값 조건 사냥 (`calculus-cubiccond`)
- 사차함수 극값 개수 세기 (`calculus-quarticcount`)
- 사차함수 도함수→원함수 매칭 (`calculus-quarticmatch`)

각 게임은 기존 8문제와 1:1 구조를 보존하면서 공통 플레이어 이름, 정확도, 개인 최고, 세션 내 플레이 순서, 즉시 재도전, 익명 행동 로그를 갖는다. `부호 변화 판독`, `개형 맞히기`, `도함수→원함수 매칭`은 모바일에서 실제 문제와 수식·그래프 보기를 확인했다.

### 기하 공통 아레나 확장

기하 단원들은 `기하_공통.js`의 `GeoLab.Arena`를 공유한다. 따라서 페이지마다 복제하지 않고 공통 아레나에 플레이 루프를 한 번 연결했다.

- 기존 8문제·3라이프·콤보·TOP 5·같은 기기 1:1 보존
- 미적분과 같은 플레이어 이름 사용
- 게임별 `geometry-{gameId}` 행동 로그 기록
- 정확도·개인 최고·세션 플레이 순서 결과 표시
- 한 번 터치 `바로 다시하기`
- 모바일 390×844에서 `스페이스 오비트` 전체 결과·재도전 확인
- 공통 생성 페이지인 `타원 스피드전`에서 이름과 시작 화면 확인

공통 아레나를 읽는 기하 단원 전체가 같은 구조를 사용하되, 각 게임의 공간 그림과 정답 생성 규칙은 기존 코드를 그대로 보존한다.

### 경제수학 8게임 및 실시간 친구방 확장

경제수학은 `JP 투자왕`과 나머지 7개 게임을 모두 같은 최소 플레이 루프로 연결했다.

- 기존 8라운드와 1% 단위 직접 배분 유지
- 매 판 사건 순서·충격 강도·표시 수치·생각해볼 질문 재조합
- 결과 화면에 첫 참가자의 이번 기록·개인 최고·세션 플레이 순서 표시
- 한 번 터치 `한 판 더`로 새로운 사건 조합 시작
- 게임별 `economy-{gameId}` 행동 로그 기록
- 실시간 친구방은 기존 Firebase 방·참가자·선택 필드를 바꾸지 않고 각 휴대폰의 행동 로그만 별도 기록
- 진행자가 재경기를 열면 참가자 휴대폰도 자동으로 다음 플레이 기록 시작

자동 검증은 `tests/economy-game-loop.test.js`에서 8게임의 라운드 수, 사건·수치·발문 변화, 결과·재경기·행동 기록 연결을 확인한다.

### 도전하기 정보 구조와 계산 스킬 Boss Battle 2.0

전역 `도전하기` 메뉴는 학생이 바로 이해할 수 있는 세 갈래로 정리했다.

- 오늘의 도전: 매일 짧게 시작하는 개인 플레이
- 친구방 LIVE: 같은 방에서 함께 하는 실시간 플레이
- 보스전: 과목별 보스를 한곳에 모으는 전용 입구

기존 미적분 아케이드와 기하 아레나는 콘텐츠를 삭제하지 않고 전역 메뉴에서만 내렸다. 과목 안의 게임으로 접근할 수 있으며, 기하 아레나는 공간을 직접 조작하는 새 프로토타입을 만든 뒤 다시 판단한다.

계산 스킬은 학생 이용 빈도가 높으므로 28개 보스를 한꺼번에 바꾸지 않고 `다항함수 빠른 미분` 하나에 첫 Boss Battle 2.0을 적용했다.

- 45초 동안 이어지는 하나의 전투
- 보스 HP 1,800과 누적 데미지
- 연속 정답 콤보와 데미지 배율
- HP 절반 이하에서 PHASE 2 철갑 방어 발동
- 난이도에 따라 철갑 방어를 뚫는 데미지 보정
- 시간 종료 시 최고 데미지 저장, 클리어 시 클리어 수 저장
- 결과 화면에서 한 번 터치로 즉시 재도전
- 기존 익명 행동 로그에 시작·완료·정확도·플레이 시간·재도전·개인 최고 기록

모바일 390×844에서 12문제 연속 정답으로 PHASE 2와 1,902 데미지 클리어를 실제 확인했다. 수식, HP, 시간, 데미지, 콤보가 겹치지 않았고 콘솔 오류도 없었다.

자동 검증은 `tests/challenge-boss-v2.test.js`에서 전역 메뉴, 보스전 모음, 투자왕 하단 중복 제거, 첫 Boss 2.0 연결과 버전을 확인한다.
