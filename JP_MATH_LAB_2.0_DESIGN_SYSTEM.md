<!-- 문서-권한 -->
> **하위 기준 · 디자인**  현재 기준은 [`JP_MATH_LAB_2.0_MASTER_SPEC.md`](JP_MATH_LAB_2.0_MASTER_SPEC.md) 하나다.
> 색·타이포·간격은 이 문서를 따른다. 구조와 정보 배치는 MASTER_SPEC 이 우선한다.
> _(2026-08-31 문서 권한 정리)_

# JP Math Lab 2.0 · Design, Motion, Math & 3D System

## 1. 디자인 원칙

JP Math Lab의 목표 인상은 “교육용 문서”가 아니라 “지금 눌러 보고 싶은 현대적인 수학 서비스”다.

가장 중요한 정보 원칙은 **“모든 것을 보여주지 않는다. 지금 필요한 것만 보여주고 나머지는 탐색하게 한다.”**이다.

- 기본 바탕은 흰색·옅은 회백색이다.
- 본문은 거의 검정에 가까운 색, 보조 설명은 중성 회색을 사용한다.
- 과목색은 큰 배경을 칠하는 용도가 아니라 상태·그래프·핵심 대상에 사용한다.
- 여백과 타이포그래피가 위계를 만들고, 테두리와 그림자는 얇고 절제한다.
- 격자는 좌표·그래프·LAB처럼 수학적으로 필요한 곳에서만 사용한다.
- 영어는 작은 분류·코드에만 쓰고, 핵심 제목과 행동은 자연스러운 한글로 표현한다.
- HOME은 카드 목록보다 한 화면에 하나의 메시지·큰 수학 오브젝트·하나의 주된 행동을 우선한다.

## 2. 공통 토큰

### 색

- Canvas: `#f6f7f5`
- Surface: `#ffffff`
- Ink: `#111816`
- Muted: `#67716d`
- Line: `#dfe4e1`
- Calculus / Motion: `#d4553f`
- Geometry / Space: `#596bd8`
- Economy / Simulation: `#168068`
- Daily / Focus: `#f0bd3f`
- Research: `#8461a9`

### 작은 글씨용 변형 (2026-08-28 추가)

과목색을 그대로 10~13px 글씨에 쓰면 흰 배경 대비가 4.5:1 에 못 미친다. 칩·코드·화살표처럼 작은 글씨에는 한 단계 누른 값을 쓰고, 아이콘·테두리·틴트에는 원래 토큰을 쓴다.

| 역할 | 도형·틴트용 | 작은 글씨용 | 흰 배경 대비 |
|---|---|---|---|
| Calculus / Motion | `#d4553f` | `#bf4630` | 5.1:1 |
| Geometry / Space | `#596bd8` | `#4c5fd0` | 5.5:1 |
| Economy / Simulation | `#168068` | `#12735d` | 5.8:1 |
| Research / 수능 탐구 | `#8461a9` | `#8461a9` | 4.9:1 |

### 과목 허브의 색 규칙 (2026-08-28 추가)

1. 한 허브에는 과목색 하나만 쓴다. 카드마다 다른 색을 주지 않는다.
2. 단원·분야 구분은 색이 아니라 단원 선택 네비게이션과 표식(`lim` · `f'` · `∫`)이 맡는다.
3. 예외는 히어로 도해다. 함수·접선·극점처럼 서로 다른 수학적 대상을 한 그림에 그릴 때만 대상별로 색을 나눈다. 이때도 과목색이 주인공이고 나머지는 잉크·중립색으로 둔다.
4. 허브의 색은 각 `index.html` 맨 위 `:root` 의 `--jp-subject` · `--jp-subject-ink` 두 값에서만 정해진다. 톤을 바꾸려면 이 두 줄만 고친다.
5. `jp-polish.css` 의 `--jp-theme` 는 위 표의 값을 따른다. 이 값이 허브의 칩·코드·태그를 `!important` 로 덮으므로, 페이지에서만 색을 바꾸면 화면에 반영되지 않는다.

### 적용 현황 (2026-08-28)

| 영역 | 이전 | 지금 |
|---|---|---|
| 미적분 허브 | 35곳에 흩어진 11종 | `--jp-subject` / `--jp-subject-ink` 2줄 |
| 기하 허브 | 21곳에 흩어진 14종 | 같음 |
| 경제수학 (허브·개념·게임) | 대단원 4색 + 개념 15색 + 장르 6색 | 같음 |

색을 유지하는 예외는 다음 세 곳뿐이다. 모두 한 그림·한 표 안에서 서로 다른 항목을 구분해야 하는 자리다.

- 미적분 히어로 도해: 함수 / 접선 / 극점
- 기하 히어로 도해: 평면 / 벡터 / 정사영
- 경제수학 자산 배분 그래프: 예금 / 채권 / 주식 / 외화 (네 값 모두 흰 배경 대비 5.4:1 이상)

### 회색 라벨 (2026-08-28 추가)

카드 번호, 개수 배지, 단원 요약, 수식 요약처럼 "작아서 흐리게" 두던 글씨도 학생이 읽는 정보다. 밝은 면 위 본문 회색은 `#616a66`(5.6:1), 크림색 카드가 많은 LIVE 화면은 `#5b645f`(6.1:1)를 쓴다. 밝은 회색은 어두운 면 위의 글씨일 수 있으므로, 값만 보고 일괄 치환하지 말고 실제 배경을 확인하고 바꾼다.

### 글꼴

- UI·게임·DAILY·LIVE: `Pretendard`, `IBM Plex Sans KR`, sans-serif
- 코드·라운드·짧은 수치: `IBM Plex Mono`, monospace
- 긴 탐구 읽기·수능 해설: 명조체를 제한적으로 사용
- 수식: KaTeX 전용 글꼴과 레이아웃을 그대로 보존

### 형태

- 작은 반경 10px, 기본 16px, 큰 카드 24px
- 일반 카드 테두리 1px
- hover 이동은 2~4px 이내
- 애니메이션은 대부분 180~350ms

## 3. Motion System

모션은 장식이 아니라 상태 변화의 설명이다.

| 상황 | 모션 |
|---|---|
| 페이지 진입 | 주요 카드가 짧게 페이드·상승 |
| 버튼 hover/press | 색·그림자·1~2% 크기 변화 |
| 탭 전환 | 선택 표시가 이동하고 내용이 짧게 등장 |
| 값 변경 | 해당 수치만 0.5초 이내 강조 |
| 저장 완료 | 짧은 체크와 성공 문구 |
| 오류 | 입력 영역의 짧은 흔들림과 원인 문구 |
| 로딩 | 콘텐츠 모양과 비슷한 스켈레톤 |
| 그래프 | 선·점·막대가 의미 있는 방향으로 변화 |

과목별 강도는 미적분 7/10, 기하 7/10, 경제수학 6/10, 교사용 3/10을 기준으로 한다.

## 4. Math Rendering System

### 2026-08-25 구현 상태

- `jp-polish.js`가 과목·수능·교사용 화면에서 로컬 KaTeX와 `jp-math-render.js`를 공통으로 준비한다.
- 이미 수식 엔진을 직접 불러오는 화면은 중복 로드하지 않는다.
- 동적으로 추가되는 문제·선택지·보스전도 MutationObserver를 통해 다시 조판한다.
- 렌더 실패는 `data-math-error="true"`와 콘솔 경고로 남기며, `JPMath.audit()`로 미표시·오버플로를 확인할 수 있다.
- 공통 렌더 대상에는 개념식, 예제식, 문제식, 러시·보스식, 선택지, 스킬식이 포함된다.

### 2026-08-28 추가

- `data-tex-raw` 는 유니코드 표기를 추정 변환하지 않고 적어 둔 LaTeX 를 그대로 조판한다. 행렬, 아래·위첨자, 분수처럼 유니코드로는 정확히 적을 수 없는 식에 쓴다.
- 요소의 본문에는 사람이 읽을 수 있는 대체 텍스트를 남겨 둔다. KaTeX 가 로드되기 전이나 실패했을 때 학생이 식을 못 보는 상태가 되지 않게 한다.
- 경제수학 15개 개념식과 실험실 결과판 라벨을 이 방식으로 옮겼다. 경제수학도 이제 공통 렌더러 안에 있으므로 `JPMath.audit()` 으로 함께 점검된다.

### 불변 규칙

1. 배포 화면의 핵심 수식을 일반 텍스트 대체물로 남기지 않는다.
2. 수식은 `data-math` 또는 공통 렌더 함수로 표시한다.
3. 동적 문제를 만든 직후 해당 영역에 공통 수식 렌더러를 다시 실행한다.
4. `.katex` 아래의 `span`에 일반 UI용 `display:block` 규칙을 적용하지 않는다.
5. 긴 수식은 모바일에서 축소하거나 가로 스크롤하되 잘라내지 않는다.
6. 수식 렌더 실패 시 원문을 숨기지 않고 오류를 개발 콘솔에 남긴다.

### 필수 검증 식

- 분수: `\\frac{x^2-1}{x-1}`
- 극한: `\\lim_{x\\to 1} f(x)`
- 미분: `f'(x)=3x^2-6x`
- 적분: `\\int_a^b f(x)\\,dx`
- 벡터: `\\vec a\\cdot\\vec b=|\\vec a||\\vec b|\\cos\\theta`
- 경우 나누기·위첨자·아래첨자·괄호가 포함된 긴 식

각 대표 화면에서 데스크톱과 폭 390px 모바일을 모두 확인한다.

## 5. 3D System

- 3D는 기하의 공간 관계를 설명할 때만 사용한다.
- 회전·확대·시점 초기화가 명확해야 한다.
- 2D 그림으로 충분한 개념에 무거운 3D를 억지로 넣지 않는다.
- 모바일 터치 회전과 페이지 스크롤이 충돌하지 않도록 조작 영역을 분리한다.
- 저성능 기기에는 정적 SVG 또는 단순 Canvas 대체 화면을 제공한다.

## 6. 대표 화면 확정 순서

1. HOME — 행동 우선 구조와 전체 서비스의 첫인상
2. 미적분 허브·DAILY — MOTION과 수식 기준
3. 기하 허브·대표 3D LAB — SPACE 기준
4. 경제수학 허브·대표 LIVE — SIMULATION 기준

네 화면을 데스크톱과 모바일에서 검증한 뒤 같은 규칙을 모든 상세 페이지에 확장한다.
